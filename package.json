{
  "name": "lapse",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Lapse — a focused interval timer PWA for workouts.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-pwa": "^1.3.0"
  }
}
